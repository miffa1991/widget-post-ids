name: CI
on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Upload to server
        uses: lattepress/WordPress-Site-Deploy@stable
        with:
          url: "21://zx407606.ftp.tools:21/"
          username: "zx407606"
          password: "EU7zG8dvBz72"
          options: --auto-init --insecure -vv
