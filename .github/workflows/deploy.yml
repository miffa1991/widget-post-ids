name: CI
on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Upload to server
        uses: lattepress/WordPress-Site-Deploy@stable
        with:
          url: ${{ secrets.SFTP_HOST }}
          username: "zx407606"
          password: "Y2m_)c2H"
